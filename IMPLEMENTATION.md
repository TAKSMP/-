# ちょうむしアプリ バトル v2 実装ガイド

taksmp/- （React + TypeScript + Vite）のバトルゲームを、
**2体2 ／ わざ3つ ／ すばやさ ／ 状態異常 ／ わざ40パターン** に拡張するための一式です。

---

## 1. この中に入っているもの（コピーするだけで動きます）

| ファイル | 中身 | 状態 |
|---|---|---|
| `src/types.ts` | 既存の型 **＋** v2用の型を末尾に追記したもの | ✅ 完成・型チェック済 |
| `src/lib/moveLibrary.ts` | わざ40パターン＋虫らしいわざ名のライブラリ | ✅ 完成 |
| `src/lib/battleEngine.ts` | バトルエンジン本体（2体2・行動順・状態異常・全効果） | ✅ 完成・200戦シミュ済 |
| `src/lib/battleSetup.ts` | 虫ごとの3わざ割り当て／旧データ移行／AIプロンプト | ✅ 完成 |
| `scripts/simulate.ts` | エンジンの動作確認スクリプト | ✅ 完成 |

**まだ書いていないのは UI（`BattlePage.tsx` の2体2対応）だけ**です。エンジンは
`resolveTurn()` 一本で呼べるようにしてあるので、Claude Code に §5 の指示を渡せば繋がります。

### 導入手順

```bash
cp -r chomushi-battle-v2/src/lib/*.ts        src/lib/
cp    chomushi-battle-v2/src/types.ts        src/types.ts   # 既存型は消していません
cp -r chomushi-battle-v2/scripts             .
npx tsc -b                                   # → エラー0を確認
npx esbuild scripts/simulate.ts --bundle --platform=node --format=esm --outfile=/tmp/sim.mjs && node /tmp/sim.mjs
```

シミュレーション結果（200戦）：**平均9.3ターン／最長20ターン／勝率ほぼ五分**。
子どもが飽きない長さに収まっています。

---

## 2. 既存データを壊さないための設計

いちばん気をつけた点です。すでにお子さんが図鑑に登録した虫は
localStorage に `battle: { hp:1〜20, attack, defense, move }` の形で入っています。

そこで **既存の `BattleStats` / `SpecialMove` / `MoveEffect` は一切変更せず**、
`BattleStatsV2` / `SpecialMoveV2` を **追加** しました。

- 旧 `BattlePage.tsx` は今までどおりコンパイル・動作します（並行稼働できる）
- `battleStatsV2(bug)` が呼ばれた時点で自動移行します
  - HP 1〜20 → 20〜60 に線形変換（`migrateHp`）
  - すばやさ … 名前とレア度から決定的に生成（毎回同じ値／`migrateSpeed`）
  - わざ … **子どもがつけたわざ名は必ず残したまま**、足りない2つを自動で足す（`mergeLegacyMove`）

> ⚠️ わざ名を残すのは意図的です。「ヘラクレスなげ」を自分で考えた子にとって、
> アップデートで名前が消えるのがいちばん悲しいので。

---

## 3. バランス設計（ここを変えれば調整できます）

`battleEngine.ts` 冒頭の定数がすべての調整ポイントです。

| 項目 | 値 | 理由 |
|---|---|---|
| HP | 20〜60 | **旧仕様の1〜20のままだと破綻します。** どく（最大HPの1/8）が HP20 では2.5ダメージにしかならず、2〜5回連続攻撃は一撃死になる。状態異常と連続攻撃を入れるならHP帯を上げるのが必須 |
| こうげき／ぼうぎょ／すばやさ | 1〜10 | 既存の⭐10段階UIをそのまま使える |
| わざ威力 | 20〜140 | ポケモン準拠。連続技は20前後、大技は120〜140 |
| ダメージ式 | `(威力/10 × (こうげき+2)) ÷ (ぼうぎょ/2+3) + 1` に ×0.9〜1.1 のゆらぎ | 威力60・こうげき7・ぼうぎょ5 で約10ダメージ＝HP40なら4発 |
| 急所 | ×1.5／確率 1/16・1/8・1/4・必中 | |
| 全体技 | ×0.75 | 2体2で単体技より弱くする定番調整 |
| 能力ランク | -6〜+6（攻防速は `(2+r)/2`、命中回避は `(3+r)/3`） | |

---

## 4. 実装済みの仕様

### すばやさと行動順
1. わざの `priority`（±）が高い順
2. 同じなら実効すばやさ（基礎値 × ランク倍率 × まひ補正0.5）が高い順
3. 同値はランダム

### 状態異常（1体につき1つまで・重複しない）

| | 効果 |
|---|---|
| どく ☠️ | ターン終了時に最大HPの1/8ダメージ |
| ねむり 💤 | 2〜4ターン行動不能。ターン数が切れると「めを さました！」 |
| まひ ⚡ | 25%で行動失敗＋すばやさ半減 |

### わざ40パターン → フラグの組み合わせで表現

40通りをswitch文で書くと破綻するので、`SpecialMoveV2` にフラグを持たせ、
エンジンが解釈する方式にしました。**新しいわざを足すときはコードを書かず
`moveLibrary.ts` に1行足すだけ**です。

```ts
// 例：2〜5回攻撃
{ hits: [2, 5], power: 20 }
// 例：姿を隠して次ターン攻撃
{ chargeTurns: 1, hideWhileCharging: true, power: 100 }
// 例：攻撃しながら相手の命中率を下げる
{ power: 55, statChanges: [{ to: 'foe', stat: 'accuracy', stage: -1, chance: 1 }] }
```

対応表（ご提示の40項目 → フラグ）は `moveLibrary.ts` の `MOVE_PATTERNS` に
1〜40の番号付きで入っています。この番号は AIプロンプトのコードにも使っています。

---

## 5. のこりの作業：`BattlePage.tsx` の2体2対応

Claude Code にそのまま渡せる指示です。

```
src/pages/BattlePage.tsx を 2体2 に対応させてください。
戦闘ロジックは src/lib/battleEngine.ts に完成しているので、新規に書かず必ずそれを使うこと。

【使うAPI】
- battleStatsV2(bug)        … src/lib/battleSetup.ts
- makeFighter(bug, stats, uid, side, photo) / createField(fighters)
- resolveTurn(field, commands) → 新しい field（field.log に演出用のテキストが入る）
- chooseCpuCommand(field, fighter) … CPU側の行動決定

【フェーズ】
1. モード選択：「1たい1」「2たい2」
2. じぶんの虫を選ぶ（2体2なら2ひき。既存の BugPicker を流用）
3. あいてを選ぶ（ランダム／じぶんでえらぶ。既存のまま）
4. じゃんけん演出は削除し、代わりに「すばやさ」で行動順が決まることを1画面で説明する
5. 戦闘：毎ターン、自分の生存中の各虫について
   「わざを選ぶ → （単体技なら）ねらう相手を選ぶ」をUIで集める。
   集めた Command[] に CPU分を足して resolveTurn を1回呼ぶ。
   返ってきた field.log を既存の演出（sleep + setVisibleLog）で1行ずつ流す。
6. 結果画面

【UI要件（こども向け・かな中心を厳守）】
- わざボタンは3つ＋「⚔️ こうげき」。各ボタンに emoji・わざ名・のこり回数
- 状態異常は虫の写真の横にバッジ（☠️どく／💤ねむり／⚡まひ）
- 能力ランクの上下は 🔺🔻 を写真の上に一瞬出す
- ステータスカードに「すばやさ」を⭐10段階で追加（StarRating を流用）
- ため中・かくれ中は写真を半透明＋⏳/🫥 を重ねる
- 2体2の配置：あいて2体を上段に横並び、じぶん2体を下段に横並び
- 対象選択は「あいての写真をタップ」で。全体技は選択させずそのまま実行

【注意】
- 既存の BattlePage は消さず、BattlePage2.tsx として新規作成 → App.tsx で切り替え、
  動作確認後に旧ファイルを削除する手順にしてください
- styles.css の既存クラス（.stage-photo, .hpbar, .stage-log など）を再利用すること
```

### AI（ChatGPT）連携の差し替え

`buildBattlePromptV2()` / `parseBattleAnswerV2()` を `battleSetup.ts` に用意済みです。
`BugDetailModal.tsx` などで旧 `buildBattlePrompt` / `parseBattleAnswer` を呼んでいる箇所を
差し替えてください。新プロンプトは「わざ3つ＋すばやさ＋わざタイプ番号(1〜40)」を
コードブロックで返させる形式で、既存の運用（コピペしてChatGPTに貼る）はそのままです。

---

## 6. 気になっている点（判断はTAKさんにお任せします）

1. **こども向けアプリとしての複雑さ。** わざ40種・能力ランク・優先度まで入れると、
   ポケモン本編に近い複雑さになります。まずは**わざ・状態異常・すばやさまでを入れ、
   「能力の奪取／入れ替え（㉝㉞）」「数ターン後に当たる（⑨）」あたりは後回し**にすると、
   遊びながら理解できる段階にできます。エンジンには全部入っているので、
   `moveLibrary.ts` から該当行をコメントアウトするだけで出し分けできます。

2. **交代システムは未実装です。** 2体2で1体倒れたらそのまま1対2で続行します。
   3体以上の控えを持たせるなら、`Field.fighters` に `bench` を足す拡張が必要です。

3. **わざの命中率演出。** 外れると子どもはストレスを感じやすいので、
   命中率は全体的に高め（90〜100）に振ってあります。

---

## 7. 動作確認のしかた

```bash
npx esbuild scripts/simulate.ts --bundle --platform=node --format=esm --outfile=/tmp/sim.mjs
node /tmp/sim.mjs
```

200戦の統計（平均ターン数・勝率）と、1試合ぶんの実況ログが流れます。
バランスを変えたら必ずこれを回して、平均8〜12ターンに収まっているか確認してください。
